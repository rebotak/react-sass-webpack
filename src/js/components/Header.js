import React from 'react';
import Navbar from './Navbar';

const Header = React.createClass({
  render() {
    return (
      <header>
        <Navbar></Navbar>
      </header>
      );
  }
});

export default Header;